version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: budget-planner
    ports:
      - "3003:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
